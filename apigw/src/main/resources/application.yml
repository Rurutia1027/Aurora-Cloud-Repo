server:
  port: ${SERVER_PORT:8083}

spring:
  application:
    name: ${APP_NAME:apigw}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: ${LOG_LEVEL:INFO}
